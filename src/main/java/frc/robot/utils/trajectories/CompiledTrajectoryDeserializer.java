package frc.robot.utils.trajectories;

import com.fasterxml.jackson.databind.ObjectMapper;
import java.io.File;
import java.io.IOException;

/**
 * Deserializer for precompiled trajectory JSON artifacts.
 * 
 * Converts JSON files generated by FeatherFlow into CompiledTrajectory objects.
 * Uses Jackson for JSON parsing and supports both file and string input.
 */
public class CompiledTrajectoryDeserializer {
    private static final ObjectMapper mapper = new ObjectMapper();

    /**
     * Deserialize a compiled trajectory from a file.
     *
     * @param file The file containing the compiled trajectory JSON
     * @return The deserialized CompiledTrajectory object
     * @throws IOException If the file cannot be read or parsed
     */
    public static CompiledTrajectory deserializeFile(File file) throws IOException {
        return mapper.readValue(file, CompiledTrajectory.class);
    }

    /**
     * Deserialize a compiled trajectory from a file path.
     *
     * @param filePath The path to the compiled trajectory JSON file
     * @return The deserialized CompiledTrajectory object
     * @throws IOException If the file cannot be read or parsed
     */
    public static CompiledTrajectory deserializeFile(String filePath) throws IOException {
        return deserializeFile(new File(filePath));
    }

    /**
     * Deserialize a compiled trajectory from a JSON string.
     *
     * @param jsonString The JSON string containing the compiled trajectory data
     * @return The deserialized CompiledTrajectory object
     * @throws IOException If the JSON cannot be parsed
     */
    public static CompiledTrajectory deserializeString(String jsonString) throws IOException {
        return mapper.readValue(jsonString, CompiledTrajectory.class);
    }
}
